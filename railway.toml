[build]
builder = "NIXPACKS"
buildCommand = "npm run install:all && npm run build && cd backend && npx prisma generate"

[deploy]
startCommand = "cd backend && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
